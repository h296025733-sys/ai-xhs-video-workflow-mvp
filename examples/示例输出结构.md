# 示例输出结构

真实视频、音频、封面和报告不会进入 GitHub。建议本地输出结构如下：

```text
outputs/
  小红书单条/
    标题__换BGM.mp4
  作者主页批量/
    01_视频成品/
      标题__abcd__BGM01.mp4
    02_封面/
      标题__abcd__cover_frame.jpg
  reports/
    xhs_direct_v2_report_YYYYMMDD.json
```

其中 `abcd` 可以来自 note_id 后四位或本地 hash 片段，用于降低同标题文件覆盖风险。
